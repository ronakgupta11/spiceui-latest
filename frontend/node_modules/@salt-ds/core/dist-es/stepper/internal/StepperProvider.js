import { jsx } from 'react/jsx-runtime';
import { createContext, useContext } from 'react';

const StepperOrientationContext = createContext("horizontal");
const StepDepthContext = createContext(-1);
function StepperProvider({
  orientation: orientationProp,
  children
}) {
  const depth = useContext(StepDepthContext);
  return /* @__PURE__ */ jsx(StepperOrientationContext.Provider, { value: orientationProp, children: /* @__PURE__ */ jsx(StepDepthContext.Provider, { value: depth + 1, children }) });
}

export { StepDepthContext, StepperOrientationContext, StepperProvider };
//# sourceMappingURL=StepperProvider.js.map
