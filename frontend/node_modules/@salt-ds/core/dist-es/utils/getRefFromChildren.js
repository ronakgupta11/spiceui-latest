import { isValidElement } from 'react';

function getRefFromChildren(child) {
  if (!child || !isValidElement(child)) {
    return null;
  }
  return Object.prototype.propertyIsEnumerable.call(child.props, "ref") ? child.props.ref : (
    // @ts-expect-error - ref is not defined on ReactNode
    child.ref
  );
}

export { getRefFromChildren };
//# sourceMappingURL=getRefFromChildren.js.map
