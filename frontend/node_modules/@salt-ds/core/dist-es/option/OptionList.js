import { jsx } from 'react/jsx-runtime';
import { clsx } from 'clsx';
import { forwardRef } from 'react';
import { makePrefixer } from '../utils/makePrefixer.js';
import { useFloatingComponent } from '../utils/useFloatingUI/useFloatingUI.js';
import '../utils/useId.js';
import '../salt-provider/SaltProvider.js';
import '../viewport/ViewportProvider.js';
import { OptionListBase } from './OptionListBase.js';

const withBaseName = makePrefixer("saltOptionList");
const OptionList = forwardRef(
  function OptionList2(props, ref) {
    const { children, className, collapsed, open, ...rest } = props;
    const { Component: FloatingComponent } = useFloatingComponent();
    return /* @__PURE__ */ jsx(
      FloatingComponent,
      {
        className: clsx(
          withBaseName(),
          {
            [withBaseName("collapsed")]: collapsed
          },
          className
        ),
        role: "listbox",
        open,
        ...rest,
        ref,
        children: /* @__PURE__ */ jsx(OptionListBase, { children })
      }
    );
  }
);

export { OptionList };
//# sourceMappingURL=OptionList.js.map
